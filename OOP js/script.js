class CssClass {
    constructor(name, index){
        this.name = name
        this.index = []
    }

    ShowStyle(){
        console.log(`${object.name}:\t${object.index}`);
    }

    AddStyle(item) {
        this.object.name.push(new object(item));
    }
    
}

let cc = new CssClass();
cc.ShowStyle();

